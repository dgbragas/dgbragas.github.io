import styled from 'styled-components';

export const Header = styled.header``;

export const Nav = styled.nav`
  align-items: center;
  display: flex;
  justify-content: space-between;
  padding: 24px 0;
`;

export const Logo = styled.img``;

export const Avatar = styled(Logo)``;
